# HANDOFF, 2026-09-22

**Current as of `2026-09-22T19:18:03Z`**, read from `date -u` at the moment the
state below was gathered.

**Every figure in this document was read from disk, from a database, or from the
provider, at that moment, and names where it was read.** Nothing here comes from
a session's memory of what it did. Where something could not be verified, it
says so and names what would settle it.

---

## 1. WHERE WE ARE

**Robert Reyna is switching Claude accounts. No conversation history survives
this document.** The next session starts with no memory of anything: not the
reasoning, not the rulings, not what was tried and abandoned. **This handoff,
`CLAUDE.md` and `BACKLOG.md` are the only context it will have.** If a fact is
not in one of those three files or derivable from the repository, it is gone.
Treat that as the operating condition rather than a caveat.

| | |
| --- | --- |
| `main` | `1343e2aa6148d1acff3065ad0fd7f17502e2c230` |
| `origin/main` | `58af89372fb658ca14d036de32560d754061c65d` |
| Do they match | **NO.** `main` is **1 commit ahead**. |
| Unpushed | `1343e2a docs(law): four rulings from 2026-09-22, and the new items ranked` |
| Working tree | **clean** (`git status --porcelain` empty) |
| Unmerged branches | **one**, `feat/design-bold`, from `git branch --no-merged main` |
| Last board | `main` at `a090ccd`, 2026-09-22: **55 PASS, 1 FAIL, 2 COULD NOT TELL**, ran to completion |
| Since that board | two commits, `58af893` and `1343e2a`, touching **only** `BACKLOG.md` and `CLAUDE.md` (`git diff --name-only a090ccd..main`). No code has changed since the board. |

**The one FAIL** is `stripe-webhook-audit`: *"the Stripe legal business name is
the registrant the board holds (Stripe says 254 Services LLC, the board's record
says 254 Engineering LLC)"*. It is an owed human read, not a code defect.

**The two COULD NOT TELL** are `mobile-overflow-audit` and `native-audit`, both
on `/portal/accounts`, the known intermittent stall. Under the operator's ruling
of 2026-09-17 these block nothing.

### THE SINGLE MOST IMPORTANT NEXT ACTION

**Push `1343e2a`, then get the operator's cropped Stripe screenshot.** The push
is his act, not a session's. The screenshot is the only thing standing between
the board and zero FAIL lines, and it is one of exactly two owed human reads.

**Do not start new work before reading section 11.**

---

## 2. THE FIRM

Every value below is cited to the file and line it was read from. The register is
`src/config/credentials.ts` and is the ONE home for all of it.

| Fact | Value | Read from |
| --- | --- | --- |
| Legal name | **254 Engineering LLC** | `src/config/business.ts:60` |
| Firm registration | **F-29811**, TBPELS | `src/config/credentials.ts:285` |
| Issued to | **254 Engineering LLC** | `src/config/credentials.ts:292` |
| DBAs on the board's record | **Sealed Engineering**, **Stamp My Plans**, **254 Engineering Services** | `src/config/credentials.ts:294` |
| Mailing address | 5601 SOUTH PADRE ISLAND DRIVE, SUITE E, CORPUS CHRISTI, TX 78412 | `src/config/credentials.ts:295` |
| Initial registration | 2026-09-10 | `src/config/credentials.ts:296` |
| Status / expires | `active` / **2027-07-31** | `src/config/credentials.ts:297-298` |
| Verified on | **2026-09-21** | `src/config/credentials.ts:299` |
| Verified by | Jessica Nassour, Licensing Specialist | `src/config/credentials.ts:300` |
| Evidence | `docs/compliance/F-29811 - 254 Engineering LLC.pdf` | `src/config/credentials.ts:302` |
| Evidence digest | `962eaffd371ae69c65113cf65e26322a97be524b715926a2dfebe2bdddfc8d99`, 709917 bytes | `src/config/credentials.ts:303-304`; the file is on disk at that path, 709917 bytes, confirmed by `ls -la` |
| Phone, E.164 | `+12819404490` | `e164Phone()` called in a child process |
| Phone, display | `(281) 940-4490` | derived, never stored |

**Derived values, called rather than copied**, each read by executing the
deriver:

- `firmName()` returns **254 Engineering LLC**
- `registrationLine()` returns **254 Engineering LLC, TBPELS Firm F-29811**
- `tbpelsFirmNumber()` returns **F-29811**

### The engineer of record

| Fact | Value | Read from |
| --- | --- | --- |
| Name | **Aman Dhakal** | `src/config/credentials.ts:178` |
| Licence number | **NOT RESTATED HERE.** It is at `src/config/credentials.ts:179` | see note below |
| Branch on the licence | Civil | `credentials.ts:196` |
| What he will seal | **structural** | `credentials.ts:197` |
| Granted / expires | 2021-12-09 / **2028-01-31** | `credentials.ts:204-205` |
| Roster status | Active | `credentials.ts:206` |
| Employers on his roster entry | `254 Services LLC`, `WILLIAMS SCOTSMAN INC.` | `credentials.ts:225` |

**Why the licence number is cited and not copied.** Standing law says never
publish the engineer's licence number, and this repository's most frequent defect
is one fact with two homes. Writing it into this document would create a second
home for a number whose whole point is that it has one. Read it at
`credentials.ts:179`. If the operator wants it restated here, that is his call
to make explicitly.

**The branch and the competence are different facts and both are recorded.** His
licence is in Civil; he seals structural only. The gate reads `sealsOnly`.

---

## 3. THE PLATFORM

**What it is.** A Next.js site and a staff portal for a Texas engineering firm
that has not opened. Three brands, one operator: 254 Engineering Services (this
repository), Sealed Engineering, and StampMyPlans. The public site sells nothing
yet; the portal runs intake, dispatch, field evidence capture, engineer review,
orders, partners, reporting and retention.

**Repository.** `https://github.com/lendbucket/254engineering`, default branch
`main`.

**Deployment.** Vercel. `254engineering.com` sits behind Vercel's bot checkpoint,
which answers `curl` with **403 and a JavaScript challenge**, not the route you
asked for. Reaching production needs a browser; the harness uses Playwright.
There is no bypass secret and none may be added.

### The databases, and the fact that matters most

Read from `supabase/projects.mjs`, which is the declared inventory:

| Role | Ref | Note |
| --- | --- | --- |
| **Production** | `fsaryeciduszuahgjbly` | declared under the name `wattsmith`. **Shared with unrelated applications.** |
| Development | `ythzaiqeoijlrdibnieo` | same schema, no real data. Every audit points here. |
| Cutover target, unused | `qmvcqvkywmkogxbyzsaz` | declared `254engineering-prod`. **0 auth users**, read 2026-09-22. Parked. |
| Rehearsal | `kmiwxtbtqrlorxfogtht` | exists and is billable; a document once wrongly said it was deleted |

The declaration also names `wattsmith-dedicated`, `salontransact`,
`Salon Envy Website` and `Lone Star Portal`, which this firm does not own.

**Production is shared, and that is why every table this firm owns is `eng_`
prefixed.** See section 10: the prefix separates tables and does not separate
`auth.users`.

**Production credentials live only in Vercel.** `.env.local` carries the
development project. The production service role key is not in the working tree
and must not be put there. A migration reaches production through the Supabase
MCP `apply_migration`, never `execute_sql`.

### Environments and Stripe

`src/config/stripe-console.ts` is a dated record of what the Stripe console
holds, because no check here can read it.

| | Value | Read from |
| --- | --- | --- |
| Account id | `acct_1UFmIjA2kbTZN5C3` | `stripe-console.ts:91` |
| Legal business name **as last read** | **254 Services LLC** | `stripe-console.ts:117` |
| Statement descriptor | `254 ENGINEERING` | `stripe-console.ts:119` |
| Support phone | `+12819404490` | `stripe-console.ts:121` |
| Read by / on | the operator, from the dashboard / **2026-09-16** | `stripe-console.ts:123-124` |

**Production uses `acct_1UFmIjA2kbTZN5C3`.** The operator confirmed on
2026-09-22 that Production's publishable key begins
`pk_live_51UFmIjA2kbTZN5C3`, which embeds that account id. **Preview formerly
carried a different company's live keys (Reyna Pay) and the operator removed
them on 2026-09-21**; that is recorded in `src/config/credential-inventory.ts`.

**The Stripe legal name is the open item.** The operator states he renamed the
account to 254 Engineering LLC on 2026-09-21, and the screenshot proving it was
deleted because it carried personal details. Until a cropped capture or a
key-run audit, `stripe-console.ts` holds the 2026-09-16 value and
`stripe-webhook-audit` is correctly red.

---

## 4. STANDING RULES

**`CLAUDE.md` is binding and is not summarised away here.** Read it in full
before writing anything. What follows is the set that most often decides whether
a piece of work is acceptable, quoted so the next session cannot mistake the
wording.

### The two above all others

> **Honesty is the ranking strategy, not a constraint on it.**

> **Nothing ships unmeasured.** Every surface goes inside an audit harness, every
> harness is verified by injecting a violation before it is trusted, and every
> completion claim is verified from disk and from a running app.

### The regulatory gate

> Nothing on this site may state or imply that engineering services are currently
> offered or performed.

> A SEALED DOCUMENT IS UPLOADED, NEVER GENERATED.

There is no seal image in this repository, no signature block, and no letter
generator, and none of the three is a gap waiting to be filled.

### The board

> **`npm run audit`, alone, as its own command, with nothing else running against
> the repository.**

> **AND THE BOARD IS RUN UNDER ITS OWN INVOCATION BEFORE ANY COMMIT IS REPORTED
> AS GREEN.**

> **A green audit is a green audit of the file it read.**

> **Every injection is read for WHICH checks went red, never only for whether the
> run failed.**

> **A CHECK WHOSE INJECTION PASSES IS A CHECK PROVEN BY NOTHING.**

> **AND A CHECK THAT HAS NEVER RUN IS NOT A CHECK. A CHECK THAT CRASHES TAKES ITS
> NEIGHBOURS WITH IT.** So anything added to an audit is run standalone before it
> is committed.

> **AN AUDIT NEVER IMPORTS ITS EXPECTATION FROM THE THING IT AUDITS.**

> **UNREACHABLE IS NOT FAILED.**

> **AND A BOARD IS BLOCKED BY A FINDING ON THE BRANCH, NEVER BY AN AUDIT THAT
> COULD NOT MEASURE SOMETHING THE BRANCH DID NOT TOUCH.**

> **AND A PREDICTION STATED IN ADVANCE AND FALSIFIED IS WORTH MORE THAN ONE THAT
> HOLDS.** Say what the run will do before running it, in terms specific enough
> to be wrong.

### Databases

> **A RUN MAY DELETE ROWS IT CREATED ITSELF, ON DEVELOPMENT, AS FIXTURE
> TEARDOWN. NOTHING ELSE.**

> **roles-audit runs against development only, and no flag overrides that.**

> **EVERY PRODUCTION MIGRATION GOES THROUGH `apply_migration`, NEVER
> `execute_sql`.**

> **A MIGRATION ON MAIN IS NEVER PENDING.**

> **A LIVE READ-BACK IS JUDGED ON COUNTS, NEVER ON THE BEHAVIOUR DIGEST.**

### Tooling, mechanical because care already failed

> **NO INLINE SCRIPT MAY CARRY A BACKSLASH.** Anything containing a backslash is
> written with the editor.

> **A GREP IN A CONDITIONAL IS A KNOWN HAZARD, NOT A MISTAKE TO AVOID BY CARE.**

> **AN INJECTION IS RESTORED BY COPY, NEVER BY `git checkout`.** Back up by copy,
> restore by copy, verify with `diff -q`.

> **A DATA-MODIFYING CTE CANNOT READ ITS OWN EFFECT.** The read-back goes in its
> own statement.

A `PreToolUse` commit guard at `.claude/settings.json` enforces several of these
mechanically. **It refused this session five times and was correct every time.**
Do not work around it.

### Session mechanics

> Feature branches. No force pushes to main. Merges only on the operator's word.

> **Read the branch off git before every merge, never off the session context.**

> **The confession rule: a completion report that is not true is the one
> unforgivable failure class.**

> **EVERY GATE REPORT INCLUDES AT LEAST ONE REAL ARTEFACT READ AS A PERSON WOULD
> READ IT.**

> **A figure offered as the extent of something says what produced it, or it is
> not offered.**

> **NO FINDING REACHES A COMMIT MESSAGE OR A REPORT UNTIL SOMETHING OTHER THAN
> THE SCRATCHPAD HAS CONFIRMED IT.**

### The operator's standing security constraints, quoted

> Never merge, never push, never apply a migration to production, never delete a
> row except fixture teardown you created on development, never send anything
> outward beyond PREVIEW-stamped sends to the operator. Never state a price the
> book does not hold. Never render an agency mark. Never publish the engineer's
> name or licence number. Never invent a turnaround figure. Never assert a
> credential the register does not hold, including in progress. Never approve a
> protocol on the engineer's behalf by any path, including a development fixture
> that could be mistaken for the real thing. Never work around a refused
> permission. Record it and continue.

> No secret key is typed into this session, ever.

---

## 5. THE LAUNCH GATE

**Nine conditions.** State below was read by calling `launchReadiness()` in a
child process at the stated timestamp, with `.env.local` loaded.

`launchMode()` returns **`trading`**. There are three modes, not two:
`prelaunch`, `trading`, `open`.

| id | State now | What closes it | Whose act |
| --- | --- | --- | --- |
| `switch` | **UNMET** | `LAUNCH_MODE=live` in the deployment | operator |
| `registration` | MET | active, unexpired F-29811 on the register | done |
| `trading-name` | MET | closed 2026-09-21 by the TBPELS reissuance | done |
| `engineer-of-record` | MET | a licensed PE with a current licence on the register | done |
| `stripe` | **UNMET** | a live charge and its refund on **production**, never a preview | operator |
| `protocols` | **UNMET** | one protocol per offered line, approved by the engineer through his own account | **the engineer** |
| `phone` | MET | `FIRM_PHONE` set; the operator confirmed it is set in Production on Vercel | done |
| `recovery` | MET | point in time recovery, enabled 2026-09-10 | done |
| `self-service-signup` | **UNMET** | the operator clears public sign up for production, by editing the file | operator |

**Four unmet, five met.**

**Two of the nine are decided by the deployment ENVIRONMENT and seven by a
file.** The environment pair is `switch` and `phone`. A process with no
deployment environment, which is what `compliance-audit` runs in, sees `phone`
unmet as well and `launchBlockers()` returns a different number there. This is
why `compliance-audit` binds the file-stated half exactly and reports the
environment it read.

**`protocols` is the one the firm cannot clear by itself.** Its blocker sentence,
read from the gate: *"No service line has a protocol approved by an engineer of
record, so all 8 are a waitlist rather than an offer."* The catalogue carries
**8** service lines (`src/content/services.ts`).

---

## 6. THE RANKED BLOCKER LIST

Each marked **code**, **configuration**, or **a person's act**.

| # | Blocker | Kind | Note |
| --- | --- | --- | --- |
| **1** | Stripe legal business name unverified | **a person's act** | The operator's cropped screenshot, or a key-run audit. The only FAIL on the last board. |
| **2** | The engineer has approved no protocol | **a person's act** (the engineer's) | 254-RC-001 v1.1 sits at `awaiting_engineer` on production. Nobody may approve it on his behalf by any path. |
| **3** | The live charge and its refund | **a person's act** | On production, as the first act after the gate opens. Never on a preview. |
| **4** | `roles-audit`'s teardown reports a cleanup it did not achieve | **code** | Ranked **first among the new items of 2026-09-22**. See section 10. |
| **5** | `LAUNCH_MODE=live` and `self-service-signup` | **configuration** | Both are deliberate edits the operator makes when the rest is done. |
| **6** | `/portal/accounts` stall | **code** | Blocks no merge under the 2026-09-17 ruling. Needs a profile captured DURING a stall. |
| **7** | The parked production cutover | **configuration**, and the operator's ruling | Included on my judgement; the reason is below. |

**Why the cutover is on this list at all.** It is not a launch blocker and the
firm can trade without it. It is here because on 2026-09-22 its cost stopped
being theoretical: another application's stranded, sign-in-capable probe accounts
are sitting in the same `auth.users` this firm authenticates against, and nothing
on either side can see the other's. It belongs **below** every item the firm
cannot trade without, and **above** nothing else, which is why it is last.

---

## 7. IN FLIGHT

### Branches

**One unmerged branch**, from `git branch --no-merged main`:

- **`feat/design-bold`** at `26f2893`, tracking `origin/feat/design-bold`. Its
  content was not reviewed in this session and I cannot say what it holds or
  whether it should merge. **What would settle it:** `git log main..feat/design-bold`
  and a board on it.

Every other branch listed by `git branch` is already merged into `main`.
`feat/photo-time-values` was merged on 2026-09-22 as `570315b`.

### Migrations

**58 migration files on disk** (`ls supabase/migrations/*.sql | wc -l`).

**No migration is pending.** `grep -n "production: null" supabase/applied.mjs`
returns only two matches, both inside explanatory comments, and neither is a
ledger entry. `schema-ledger-audit` passed on the last board, which is the check
that fails when a migration is reachable from `main` and the ledger says
production lacks it.

The three most recent, all applied to production and read back:

| Migration | Applied | Provider version |
| --- | --- | --- |
| `0055_the_signed_roof_protocol_awaits_its_engineer` | 2026-09-21 | `20260921205424`, **recorded WITHOUT its numeric prefix**, a known slip |
| `0056_the_engineer_states_the_discipline` | 2026-09-22 | `20260922134120` |
| `0057_a_photograph_carries_three_times` | 2026-09-22 | `20260922155844` |

0057 was read back on production before and after: shape moved
`56b351a693ec70cc86203fd0cbde481c` at 1,141 columns to
`e2bc81c9096a0eb4d8b8366ce3aea881` at **1,143** columns, which is the ledger's
declared fingerprint exactly and exactly two more columns. Nothing was
backfilled: `eng_evidence_items` holds 0 rows on production.

### Acknowledgements with expiries

| What | Expires | What happens then |
| --- | --- | --- |
| The engineer's roster entry still names `254 Services LLC` | **end of 2026-09-24** | `compliance-audit` FAILS from 2026-09-25 unless the roster is re-read and records `254 Engineering LLC`. Recorded at `src/config/credentials.ts:247`. |

**This is the only acknowledgement in the system.** It is reported as
`ACKNOWLEDGED`, not PASS, in its own block with the date on the line.

### Checks red or acknowledged, from the last board

- **FAIL**, 1: `stripe-webhook-audit`, the Stripe legal name.
- **ACKNOWLEDGED**, 1: `compliance-audit`'s roster entry check, above.
- **COULD NOT TELL**, 2 audits: `mobile-overflow-audit` and `native-audit`, both
  `/portal/accounts`.

### Ruled but not done

1. **`roles-audit`'s teardown.** Ruled on 2026-09-22 **not** to be fixed yet and
   to be ranked first among the new items. It is.
2. **Telling whoever owns wattsmith that its audit strands probes.** Ruled
   2026-09-22 to be **the operator's to send**, not a session's.
3. **The retention questions have not been sent to the engineer.**
   `docs/retention-questions-for-the-engineer.md` is accepted as written and the
   operator will send it **after** the engineer's protocol approval, never with
   it, so that his approval stays a single act.

---

## 8. RULINGS OWED, WITH RECOMMENDATIONS

1. **Does `feat/design-bold` merge, or is it abandoned?** It is the only
   unmerged branch. *Recommendation:* read `git log main..feat/design-bold`
   first; do not merge anything carrying a migration without a production
   sitting.
2. **Fix `roles-audit`'s teardown now, or after launch?** *Recommendation:* fix
   it before the next unattended run. It is the only item on the ranked list
   that has already caused the thing it exists to prevent.
3. **Does the commit guard learn to read the ledger?** *Recommendation:* yes,
   eventually, and not urgently. The class fix refuses only a migration declared
   `production: null`. Do not exempt merge commits wholesale.
4. **The cutover.** *Recommendation:* keep it parked until the firm trades. The
   argument for moving is now concrete but the cost of moving mid-launch is
   worse.
5. **Should the licence number be restated in this handoff?** *Recommendation:*
   no. It is cited to file and line in section 2.

---

## 9. THE PROTOCOL

**254-RC-001, "Roof Condition Assessment", version 1.1.**

| | |
| --- | --- |
| Transcription | `src/content/protocols/rc-001.ts`, with `rc-001-intake.ts`, `rc-001-checklist.ts`, `rc-001-decisions.ts` |
| v1.1 digest | `d050a21a2b2d43114de47989ca731f41e26951f195c90187e99c11705011e4ef` (`rc-001.ts:44`) |
| v1.0 digest | `f6d3ca925ddcb305292895c47249199ce86977644d3323d8a0d8ccc017803ba9` (`rc-001.ts:36`) |
| v1.1 issue date | 2026-09-18 (`rc-001.ts:40`) |
| Approval page date | 2026-09-20 (`rc-001.ts:84`) |

### Its state on production, read 2026-09-22

Read from `eng_protocol_templates` on `fsaryeciduszuahgjbly`:

| Column | Value |
| --- | --- |
| `document_number` | `254-RC-001` |
| `version_label` / `version` | `1.1` / `2` |
| `status` | **`awaiting_engineer`** |
| `requires_discipline` | **`structural`** |
| `document_signed_at` | `2026-09-20` |
| `approved_by`, `approved_at`, `published_at` | **all null** |
| `created_at` | `2026-09-21 20:54:24` |

**v1.0 is deliberately not in the table.** Operator ruling: only the version in
force is loaded.

### What approval does

Approval moves the row to `published`, names the approver and the time, and is
the act that closes the `protocols` launch condition for the roof line. **Only
the engineer can do it, and only through his own account.** Standing law forbids
approving on his behalf by any path, *"including a development fixture that could
be mistaken for the real thing"*.

`awaiting_engineer` exists because none of `draft`, `published` or `retired` was
true of a document he had signed but not approved in the platform, and three
check constraints enforce the distinction.

### Still open with the engineer

Recorded in `src/config/engineer-directions.ts`, which holds three directions
from his emailed reply, each with what it establishes and what remains owed:

1. Technician qualification, accepted.
2. Roof certification discipline: he stated `structural`, now on production.
3. Desktop and solar letter conditions.

Plus, not yet sent: **the retention questions**,
`docs/retention-questions-for-the-engineer.md`, ten questions in three groups,
to go to him **separately from any approval**.

---

## 10. KNOWN DEFECTS AND OPEN QUESTIONS

### The `/portal/accounts` stall

Two timeout ceilings, both observed: **45,000 ms** in `native-audit` and
**90,000 ms** in `mobile-overflow-audit` and `contrast-audit`. The screen has
been measured taking **fifty seconds of application time**, read off the
server's own log, and on 2026-09-17 the identical code measured fine on a later
board with nothing fixed in between.

**It is intermittent, which changes what a profile must do.** A profile of a
healthy render is not evidence about a stall and reads exactly like one. The
instrumentation has to already be running when a stall arrives. `accountRows()`
paging every service order belonging to any account is a plausible cause
**nobody has profiled**.

### The roll-up undercount

An audit that adopts the third verdict removes itself from the roll-up's count
while keeping its own honest line, and **nothing reconciles the two**. It is
ranked **first in Phase 14 survey 3**, because it hides misses from the summary
people actually read. Reasoning in `BACKLOG.md` around the Phase 14 ranking.

### The Phase 14 surveys, with their ranking

`docs/phase-14-surveys.md`, all report only:

- **Survey 1, status functions returned as user-facing errors.** Nearly empty,
  which is the result. Two sites remain in `src/lib/ops-mfa.ts`, both currently
  complete. `ops-mfa.ts:341` is the one to fix first.
- **Survey 2, boundaries comparing clocks across sources.** One real finding on
  a money path: a statement's `issued_at` comes from the database clock and its
  `due_at` from the application clock. **Fixing it the obvious way creates a
  third defect** downstream.
- **Survey 3, recovery paths proven by nothing.** Ranks 1 to 3 are the restore,
  point in time recovery, and the `ALLOW_PRODUCTION_DB` permitted path.

**Standing ranking:** the `/portal/accounts` stall goes ahead of the surveys;
the roll-up undercount goes first within survey 3.

### `roles-audit`'s teardown, ranked first among the new items

`scripts/roles-audit.mjs` carries its own probe domain and its own teardown, and
has all three layers of the defect fixed in `scripts/lib/portal-probe.mjs` on
2026-09-22:

| | `destroyProbes`, fixed | `roles-audit` teardown, **not fixed** |
| --- | --- | --- |
| Subject | profiles AND auth users on the domain | only the ids THIS run made, so a crashed run strands rows |
| `deleteUser` failure | surfaced and reported | `.catch(() => {})`, swallowed |
| Verification | counts BOTH | counts `eng_profiles` only |

**It is the function that actually stranded two accounts**,
`probe-field_tech-1788582223356` from 2026-09-05 and
`probe-cap_492250-1789391492250` from 2026-09-14, profiles swept, auth users
alive seventeen and eight days later. Both were deleted on the operator's ruling
of 2026-09-22 and the deletion is recorded in `eng_audit_events` on development
as events 25547 and 25548, beside the permanent sign-in event 2372.

Its check is named **"probe accounts were removed"** and reads green while
sign-in-capable accounts remain, because the verification reads the table the
deletion already cleared.

### The five Wattsmith probes on the shared `auth.users`

**Observed on production `fsaryeciduszuahgjbly` on 2026-09-22. Not touched, by
operator ruling.**

- Five accounts: `support+os-audit-tech`, `-office`, `-master`, `-owner`, `-hq`,
  all `@wattsmithelectric.com`
- Created **2026-08-05**, within three minutes of each other
- **No `eng_profiles` row** for any of the five
- Made by the **wattsmith application's own audit**, not by anything in this
  repository

**Its audit strands them, and nobody has told whoever owns it.** The operator
ruled on 2026-09-22 that telling them is **his to send**, not a session's. As of
this handoff it has not been sent.

**They carry no `eng_profiles` row, so every check this firm owns is correct to
ignore them** — and would go on being correct while they sat there. They are
real, sign-in capable, and live in the same `auth.users` this firm's portal
authenticates against.

**`auth.users` is the one namespace the `eng_` prefix cannot separate.** The
prefix separates TABLES. It does not separate the table that decides who can
sign in. This firm found the identical defect in its own audit on the same day,
and neither side could see the other's.

**This is a live cost of the parked cutover**, not an argument about tidiness. A
project of this firm's own is what ends it. The deferral is in
`docs/production-cutover-plan.md`.

### What I could not verify

- **Whether the Stripe account's legal name is now 254 Engineering LLC.** Only
  the operator's cropped screenshot or a run with the live key would settle it,
  and no secret key may be typed into a session.
- **Whether the engineer's TBPELS roster entry has updated.** Only a fresh
  roster read by a person settles it. Nothing here can read the roster.
- **What `feat/design-bold` contains.** A diff and a board would settle it.
- **Whether the `/portal/accounts` stall is `accountRows()`.** Only a profile
  captured during a stall settles it.

---

## 11. HOW TO PICK THIS UP

### The first five commands

Run these before doing anything else. They establish state from git, the ledger
and the provider rather than from this document, which is already going stale.

```
1.  git fetch origin && git status && git log --oneline origin/main..main
2.  git branch --no-merged main
3.  npx tsx scripts/schema-ledger-audit.mjs
4.  npx tsx scripts/compliance-audit.mjs
5.  npm run audit          # alone, nothing else running, ~20 minutes
```

Command 3 answers "does the repository agree with itself about what production
has". Command 4 answers "may the gate change at all", and prints the
acknowledgement with its expiry. Command 5 is the only thing that knows whether
the rest of the repository still agrees.

**Then read, in this order:** `CLAUDE.md` in full, `BACKLOG.md`, and
`docs/new-surface-checklist.md` if you are about to build a portal screen, an
API route, or a table.

**To confirm production's migration history**, which no check in the repository
can read: Supabase MCP `list_migrations` against `fsaryeciduszuahgjbly`.

### The order of work

1. **Push `1343e2a`** — the operator's act.
2. **Chase the two owed human reads**: the Stripe legal name and the roster
   re-read. The roster one has a **deadline of 2026-09-24**, after which
   `compliance-audit` goes red on it.
3. **Fix `roles-audit`'s teardown** before any long unattended run.
4. Only then take new feature work.

### How to work here

- One thing at a time, committed on its own, with the audits that read what you
  touched run standalone and their exit codes read.
- **Derive which audits read a file you changed** (`grep -rln "<file>" scripts/`)
  rather than guessing. Guessing cost this session three red boards.
- Predict the board in writing before running it, specific enough to be wrong.
- Report what happened, including what did not work. The confession rule is the
  one unforgivable failure class.

---

**Written 2026-09-22 as the final act of the session on the previous Claude
account. `main` at `1343e2a`, one commit ahead of `origin/main`, tree clean.**
